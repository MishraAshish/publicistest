import React from "react";

export const Header = (props) => {
        return(
            <header className="header">
                <h1>ATM Money Dispenser</h1>
            </header>
        );
    };